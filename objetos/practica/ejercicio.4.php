<?php

/**
 * En la clase Producto agregar el atributo privado $descuento.
 * Cada vez que un producto nuevo se crea el descuento tiene que estar establecido en 0.
 * Agregar el método aplicarDescuento.
 * El método debe recibir un número entero que representa el porcentaje de descuento.
 * 
 * Modificar el getter de precio para devolver el valor correcto en función del descuento.
 */

// $top = new Prenda('Top', 3500, 'Blanco', 'M');
// $top->aplicarDescuento(10);
// $top->getPrecio(); // 3150

