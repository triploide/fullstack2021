<?php

/**
 * Crear la clase Electrodomestico que herede de la clase Producto.
 * La clase Electrodomestico debe recibir un precio, un nombre y un consumo (El atributo consumo es una letra que va desde la A a la F). 
 * Crear también la clase Prenda, que herede de la clase Producto.
 * La Clase Prenda, además de precio y nombre, debe recibir un talle y un color.
 * Agregar tanto en la clase Electrodomestico como en la clase Prenda un método mostrar info que muestre por pantalla sus características.
 * ¿Hay alguna forma de optimizar el método usando la clase Producto?
 */

// $heladera = new Electrodomestico('Heladera HPK 135', 70000, 'A');
// $top = new Prenda('Top', 3500, 'Blanco', 'M');

// $heladera->mostrarInfo(); // Heladera HPK 135, precio: 70000, consumo: A
// $top->mostrarInfo(); // Top, precio: 3500, color: Blanco, talle: M

