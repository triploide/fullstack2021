<?php

/**
 * Crear la clase Producto con los atributos nombre y precio.
 * La clase debe tener un constructor que permita asignar valores a los atributos.
 * Los atributos deben ser privados.
 * Crear los getters para poder obtener el valor de los atributos.
 * 
 */

// $producto = new Producto('Taza', 10);
// $producto->getNombre(); // Taza
// $producto->getPrecio(); // 10