<?php

/**
 * Crear la clase Carrito.
 * La clase Carrito va a tener un atributo privado llamado productos.
 * No es necesario crear un constructor.
 * La clase Carrito va a tener el método agregarProducto que recibe un objeto de la clase Producto y lo agrega al atributo productos.
 * A su vez, Carrito va a tener un método llamado getTotal que devuelve el precio total de todos los productos que hay en el carrito.
 * Carrito debe calcular el total teniendo en cuenta que el producto puede tener un descuento.
 */

// $heladera = new Electrodomestico('Heladera HPK 135', 70000, 'A');
// $top = new Prenda('Top', 3500, 'Blanco', 'M');
// $heladera->aplicarDescuento(10);

// $carrito = new Carrito;
// $carrito->agregarProducto($heladera);
// $carrito->agregarProducto($top);
// $carrito->getTotal(); // devuelve 63000 + 3500 = 66500
